# ===============================
# Order System – Environment Template
# Copy to `.env` and replace placeholder values.
# Notes:
# - Do NOT commit your real `.env`.
# - Strings with spaces or special chars should be quoted.
# - Booleans should be true/false (lowercase).
# ===============================

# ===== Database & Auth =====
# Connection string to your MongoDB database (Local/Atlas).
# e.g. mongodb://localhost:27017/order-system  or  mongodb+srv://user:pass@cluster/db
MONGO_URI="your-mongo-uri"

# Secret used to sign JWT tokens. Keep this long and random.
JWT_SECRET="your-jwt-secret"

# ===== AWS (S3 uploads, etc.) =====
# AWS IAM access keys for the account/user with S3 permissions.
AWS_ACCESS_KEY_ID="your-access-key"
AWS_SECRET_ACCESS_KEY="your-secret-key"
# AWS region of your S3 bucket (e.g., us-east-1, ap-southeast-1).
AWS_REGION="ap-southeast-1"
# Name of the S3 bucket used by the app.
AWS_S3_BUCKET_NAME="order-system-bucket"

# ===== Payments (Stripe) =====
# Secret key for server-side Stripe API calls (starts with sk_...).
STRIPE_SECRET_KEY="your-stripe-secret"
# Webhook signing secret for validating Stripe webhooks (from Stripe Dashboard).
STRIPE_WEBHOOK_SECRET="your-webhook-secret"
# Public key used by the frontend to initialize Stripe (starts with pk_...).
VITE_STRIPE_PUBLISHABLE_KEY="your-stripe-publishable-key"

# ===== App URLs =====
# Deployed Admin app URL (used in emails/links).
ADMIN_URL="https://your-admin-url.com"
# Deployed Customer app URL (used in emails/links).
CUSTOMER_APP_URL="https://your-customer-url.com"
# Local Admin URL for development (e.g., Vite dev server).
LOCAL_ADMIN_URL="http://localhost:5173"

# ===== Server =====
# Port your backend server listens on.
PORT=5000

# ===== Admin Access (basic app guard / bootstrap) =====
# Initial admin credentials used by the app (only for development).
ADMIN_ACCESS_USERNAME="admin"
ADMIN_ACCESS_PASSWORD="password"

# ===== Environment Flags =====
# Node runtime environment: development | production | test
NODE_ENV="development"
# Your own local flag if used by the codebase.
LOCAL_ENV="development"
# If true, features that require an active subscription are enforced.
ENFORCE_SUBSCRIPTIONS=true

# ===== Frontend Config (Vite reads vars prefixed with VITE_) =====
# Base API URL used by the Admin frontend.
VITE_API_BASE_URL="http://localhost:5000/api"
# Base API URL used by the Customer frontend (public endpoints).
VITE_API_BASE_CUS_URL="http://localhost:5000/api/public"
# WebSocket URL used by frontends.
VITE_WS_URL="ws://localhost:5000"

# ===============================
# Tips:
# - Keep `.env` out of version control; commit only `.env.example`.
# - For production, rotate STRIPE/MONGO/JWT secrets and store them in your host/CI or an env manager.
# - If you later unify to a single root `.env`, these names can remain the same.
# ===============================
